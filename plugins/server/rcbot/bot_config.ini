config commanding 0
config wait_for_orders 1
config dont_shoot 0
config chatting 0
config real_mode 1
config max_bots 3
config min_bots 3
config gorge_amount 0.4
config update_ladder_time 0.0
# max path revs, the lower the setting
# the better CPU performance in the
# path finding algorithm, but it takes
# longer to find paths.
config max_path_revs 900
# update vision revs, time to update what
# the bot can see, as it is quite cpu
# intensive you can change these values 
# depending on cpu speed.
config max_update_vision_revs 300
config update_vision_time 0.01
# dont reserve bot slots
config reserve_bot_slots 0
# bots old chat chance
config chat_percent 100
# bots reply chat chance using megahal
config chat_reply_percent 95
# keep bots learning their chatting skills
config chat_dont_learn 0
# bots try to balance teams
config balance_teams 1
# dont build ip at NS start
config marine_auto_build 0

# Optional settings, remove # tags to use...

# My (madmax2) prefered setting for ladder climbing, may help bots wiggle free from sticky ladders
# config update_ladder_time 0.2

# If bot gets hopelessly stuck, it may suicide a bit faster, increase to 50 for quicker suicides
# config bot_stuck_speed 30

# On a listenserver, could bind keys to "exec xxxx.cfg" with "rcbot config sc_breakable_classes_ally"
# with or without the 0, to turn on and off, or make a map.cfg for every map with it on or off
# see included polar_rescue.cfg in the map_configs folder for example cvar syntax

config tooltips 0